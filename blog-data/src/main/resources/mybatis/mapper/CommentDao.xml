<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.jxufe.dao.CommentDao">

    <insert id="insertComment">
        insert into comment(content, blog_id, user_id, reply_id, parent_id)
        value (#{cntent}, #{blog_id}, #{user_id}, #{reply_id}, #{parent_id})
    </insert>
    <delete id="deleteComment">
        delete
        from comment
        where id = #{commentId}
        limit 1
    </delete>
    <update id="updateCommentContent">
        update comment
        set content=#{content}
        where id = #{commentId}
        limit 1
    </update>

</mapper>